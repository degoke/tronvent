apiVersion: v1
kind: Service
metadata:
  name: {{ include "tronvent.fullname" . }}
  labels:
    {{- include "tronvent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tronvent.selectorLabels" . | nindent 4 }}
